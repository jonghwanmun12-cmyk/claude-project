# 09 — 로마 왕복 기본값 프리셋

## Outcome

저장된 일정이 없는 첫 방문 시, 도착 도시·출발 도시가 모두 로마로, 도착 일시가 2027-05-23 19:00, 출발 일시가 2027-05-30 21:00으로 미리 채워진 채 화면이 열린다. 사용자는 이 값들을 계속 자유롭게 수정할 수 있다.

## Blockers

None.

## Acceptance criteria

- [ ] localStorage에 저장된 일정이 없고 공유 링크로 들어온 것도 아닐 때, 도착 도시·출발 도시 select가 모두 "로마"로 선택되어 있다.
- [ ] 같은 조건에서 도착 일시 입력칸이 2027-05-23T19:00, 출발 일시 입력칸이 2027-05-30T21:00로 채워져 있다.
- [ ] 도착/출발 도시나 일시를 다른 값으로 바꾸면 정상적으로 바뀌고, 이후 새로고침해도(같은 브라우저) 바뀐 값이 유지된다(기존 localStorage 저장 동작 그대로).
- [ ] 공유 링크로 들어오거나 이미 저장된 일정이 있는 경우에는 이 기본값 대신 기존처럼 공유/저장된 값이 표시된다(회귀 없음).

## Constraints

None.

## Verification

- 저장된 일정 없이 처음 렌더링했을 때 기본값 4가지(도착 도시/일시, 출발 도시/일시)가 기대값과 일치하는지 테스트.
- 기존 공유 링크/저장된 일정 우선순위 동작이 깨지지 않았는지 기존 테스트로 확인.

## Review checkpoint

None.

## Status

completed

## Execution

- Verification: `bun run test` 70개 통과(신규 "저장된 일정이 없으면 로마 왕복 기본값이 미리 채워진다" 테스트 포함). 기존에 출발 도시 기본값(베네치아)에 암묵적으로 의존하던 테스트 4개는 출발 도시를 명시적으로 "venice"로 선택하도록 수정해 회귀를 막았다.
- Blocker: 없음.
- Revision: 없음.
