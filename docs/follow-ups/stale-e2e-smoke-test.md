# e2e 스모크 테스트가 템플릿 기본 화면을 기준으로 남아 있다

`e2e/smoke.spec.ts`가 "Create Next App" 타이틀과 "To get started" 제목을
검사하는데, `app/page.tsx`는 이미 오래전에 이탈리아 여행 플래너 화면으로
바뀌어 있다. 이번 세션(지도·후보군·디자인 리프레시 작업)에서 만든 변경과는
무관한 기존 결함이라 그대로 두고 기록만 남긴다. `bun run test:e2e`를 실제로
쓰려면 이 테스트를 현재 화면 기준으로 다시 작성해야 한다.
